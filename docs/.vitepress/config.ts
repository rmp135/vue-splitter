import { defineConfig } from 'vitepress'

export default defineConfig({

  title: 'VueSplitter',
  base: '/vue-splitter/',
  
  themeConfig: {
    socialLinks: [{
      link: 'https://github.com/rmp135/vue-splitter',
      icon: 'github'
    }]
  }
})